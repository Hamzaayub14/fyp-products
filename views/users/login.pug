extends ../layout

block content
  h1 Login
    
  form(action="/login", method="post")
    
        .form-group 
            label(for="email") Email 
            input#title(type="text", class="form-control" name = "email" value="hamza.ayub.2")
        .form-group 
            label(for="password") Password 
            input#price(type="password", class ="form-control" name ="password" value="hamzaayub" )
            br
        input(type="submit", class="btn btn-info" value="Login")